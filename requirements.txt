# ── Core ML ───────────────────────────────────────────────────────────────────
torch~=2.6.0
torchvision~=0.21.0
lpips==0.1.4
pytorch_msssim==1.0.0

# CUDA wheels (Windows only — swap cu121 for your driver version if needed)
torch==2.6.0+cu121; sys_platform == "win32"
torchvision==0.21.0+cu121; sys_platform == "win32"

# ── Web UI ────────────────────────────────────────────────────────────────────
gradio>=5.0.0

# ── Diffusion models ──────────────────────────────────────────────────────────
diffusers>=0.38.0
transformers>=4.50.0
accelerate>=1.5.0

# ── Image processing ──────────────────────────────────────────────────────────
Pillow>=11.1.0
opencv-python>=4.9.0
scikit-image>=0.25.0
imageio>=2.37.0
tifffile>=2025.3.0

# ── Numerics / utilities ──────────────────────────────────────────────────────
numpy>=1.26.0,<2.0
scipy>=1.15.0
numba>=0.61.0
llvmlite>=0.44.0
matplotlib>=3.10.0

# ── Screen capture & window management ───────────────────────────────────────
mss>=10.0.0
pyobjc-framework-Quartz; sys_platform == "darwin"
PyGetWindow~=0.0.9; sys_platform != "darwin"
PyRect>=0.2.0; sys_platform != "darwin"
pywin32>=310; sys_platform == "win32"
